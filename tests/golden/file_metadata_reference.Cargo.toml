[package]
name = "fmref"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "fmref"
path = "src/main.rs"

[dependencies]
ciborium = "0.2.2"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
